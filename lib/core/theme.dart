import 'package:flutter/material.dart';

class AppTheme {
  // Primary Colors
  static const Color primaryColor = Color(0xFF6A1B9A);
  static const Color secondaryColor = Color(0xFF512DA8);
  static const Color accentColor = Color(0xFFFFC107);

  // Background & Text Colors
  static const Color backgroundColor = Color(0xFF09001F);
  static const Color cardColor = Color(0xFF1E1E1E);
  static const Color textColor = Colors.white;
  static const Color greyText = Color.fromRGBO(255, 255, 255, 0.45);
  static const Color mutedTextColor = Colors.grey;

  // Icon color
  static const Color iconSelectedColor = Color(0xFF6A1B9A);
  static const Color iconUnselectedColor = Color.fromRGBO(255, 255, 255, 0.45);

}
